import { Component } from '@angular/core';

@Component({
  selector: 'app-profile',
  standalone: true,
  template: `<div class="profile-placeholder">Perfil de usuario (en desarrollo)</div>`,
  styles: [`.profile-placeholder { max-width: 600px; margin: 48px auto; font-size: 1.3rem; color: #1976d2; text-align: center; font-family: 'Montserrat', sans-serif; }`]
})
export class ProfileComponent {} 